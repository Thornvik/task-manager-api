module.exports = {
  setApiKey() {}, // mocking the setApiKey function
  send() {}, // mocking the send function
  // there is no need to return anything from these since we dont do anything with the return value
}
